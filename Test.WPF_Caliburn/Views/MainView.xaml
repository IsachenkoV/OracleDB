<Window x:Class="Test.WPF_Caliburn.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:views="clr-namespace:Test.WPF_Caliburn.Views"
        Title="MainView" Height="300" Width="300">
    <Grid>
        <TabControl>
            <TabItem Header="Box">
                <TextBox Text="{Binding Stub, UpdateSourceTrigger=PropertyChanged}"></TextBox>    
            </TabItem>
            
            <TabItem Header="Button">
                <Button Content="Click" cal:Message.Attach="DoMagic"></Button>    
            </TabItem>
            
            <TabItem Header="{Binding Stub}">
                <ContentControl cal:View.Model="{Binding User}"/>    
            </TabItem>
            
            
        </TabControl>
        
    </Grid>
</Window>
